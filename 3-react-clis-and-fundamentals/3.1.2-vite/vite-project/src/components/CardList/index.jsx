import React from "react";

function CardList({ children }) {

    // const children = props.children;
    // const { children } = props;

    return (
        <React.Fragment>
            {children}
        </React.Fragment>
    )
}

export { CardList };